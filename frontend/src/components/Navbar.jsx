/**
 * Global navigation bar.
 *
 * Renders different nav links depending on the current user's role:
 *   guest    — Login, Register
 *   student  — Dashboard, My Courses, Calendar
 *   lecturer — Dashboard, My Courses, Assignments
 *   admin    — Dashboard, All Courses, Reports, Users
 *
 * Includes a Logout button that calls logout() from AuthContext.
 *
 * Owner: Camarly Thomas
 */
