/**
 * Thread list component.
 *
 * Renders a list of forum thread summaries (title, author, reply count, timestamp).
 * Each item links to the full ThreadPage.
 *
 * Props:
 *   threads   Array<{ id, title, created_by_username, reply_count, created_at }>
 *
 * Style input requested from: Tramonique Wellington
 * Owner: Camarly Thomas
 */
