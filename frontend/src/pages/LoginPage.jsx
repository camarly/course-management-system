/**
 * Login page.
 *
 * Contains:
 *   - Username + password form → calls login() from auth API → stores JWT → redirects to /dashboard
 *   - "Sign in with Google" button → calls googleLogin() which redirects to /api/auth/google/login
 *   - Link to /register
 *
 * Owner: Camarly Thomas
 */
