"""
Pytest configuration and shared fixtures.

Fixtures defined here are available to every test module automatically.
Add the Flask test client, seeded test data helpers, and auth token
factories here so individual test files stay clean.
"""

import pytest
