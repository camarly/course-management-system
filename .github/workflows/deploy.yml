name: Deploy

# Deployment is disabled until a hosting provider is chosen.
# This workflow only runs when triggered manually from the Actions tab.
# Re-enable automatic deploys by adding a `push` trigger on `main` once the
# provider-specific steps below are filled in.

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy (disabled placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Deployment provider not yet chosen. This workflow is a placeholder — edit .github/workflows/deploy.yml when ready."
